{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "Polyfrost"
  ],
  "environment": "client",
  "depends": {
    "minecraft": "${mc_version}",
    "oneconfig-compose-bundle": "*"
  }
}